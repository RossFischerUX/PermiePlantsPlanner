---
phase: 02-functional-data-enrichment
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/(app)/plants/[id]/page.tsx
  - tests/plants.spec.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "A plant detail page shows functional role tags from permaculture_uses as terracotta pills"
    - "A plant detail page shows forest layer (InfoCell) + succession role (pills)"
    - "A plant detail page shows establishment difficulty, maintenance level, years to bearing (InfoCells) + propagation methods (pills)"
    - "A plant detail page shows edible parts + harvest months (pills, months in calendar order)"
    - "Empty/null fields are conditionally hidden — never labeled N/A"
    - "D-05: the generic 'Permaculture' section is retired and its content redistributed into 4 dedicated sections — Functional Roles / Forest Layer & Succession / Establishment & Care / Harvest"
    - "D-06: array/tag fields render as rounded-full pills; scalar fields render as InfoCell rows"
    - "D-07: each section uses the eyebrow class text-[11px] font-semibold text-warm-stone uppercase tracking-[0.06em], slots after Landscaping and before Notable Cultivars, and conditionally hides null/empty fields via && guards"
  artifacts:
    - path: "app/(app)/plants/[id]/page.tsx"
      provides: "4 new dedicated sections replacing the retired Permaculture section"
      contains: "Forest Layer & Succession"
    - path: "tests/plants.spec.ts"
      provides: "Playwright assertions for the 4 new sections + conditional-hide"
      contains: "Functional Roles"
  key_links:
    - from: "app/(app)/plants/[id]/page.tsx"
      to: "lib/plant-labels.ts MONTH_OPTIONS + InfoCell label consts"
      via: "import for harvest_months calendar ordering + InfoCell labels"
      pattern: "MONTH_OPTIONS"
    - from: "detail page sections"
      to: "plant.<new fields>"
      via: "RSC reads existing .select('*') — no query change"
      pattern: "plant\\.(succession_role|edible_parts|harvest_months|propagation_methods)"
---

<objective>
Replace the retired generic "Permaculture" section in `app/(app)/plants/[id]/page.tsx` with four dedicated read-only sections (Functional Roles, Forest Layer & Succession, Establishment & Care, Harvest) per the approved UI-SPEC, and add Playwright assertions covering rendering + conditional-hide behavior.

Purpose: This is the display slice — after this plan a real user visiting a plant detail page sees the structured functional data the pipeline populated. Closes the user-facing half of DATA-01..DATA-04. UI is reuse-only (byte-identical existing InfoCell/pill markup).
Output: Restructured detail page (4 sections), extended `tests/plants.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-functional-data-enrichment/02-CONTEXT.md
@.planning/phases/02-functional-data-enrichment/02-UI-SPEC.md
@.planning/phases/02-functional-data-enrichment/02-PATTERNS.md
@.planning/phases/02-functional-data-enrichment/02-02-SUMMARY.md

<interfaces>
<!-- app/(app)/plants/[id]/page.tsx — REUSE byte-identical. Do NOT change the Supabase query (line 22-26 .select('*') already returns new columns). -->

InfoCell component (lines 9-16 — reuse verbatim, no change):
```tsx
function InfoCell({ label, value }: { label: string; value: string }) {
  return (
    <div className="bg-stone-white rounded-xl p-4">
      <p className="text-[11px] font-semibold text-warm-stone uppercase tracking-[0.05em] mb-1">{label}</p>
      <p className="text-sm text-dark-bark capitalize">{value}</p>
    </div>
  )
}
```

Pill chip markup (lines 162-164 — reuse BYTE-IDENTICAL classes for all 5 array fields):
```tsx
<span key={x} className="text-xs bg-terracotta/10 text-terracotta px-3 py-1.5 rounded-full border border-terracotta/20 capitalize">{x}</span>
```

Section wrapper + eyebrow (lines 128-129):
```tsx
<section className="mb-8">
  <h2 className="text-[11px] font-semibold text-warm-stone uppercase tracking-[0.06em] mb-4">TITLE</h2>
```
InfoCell grid: `grid grid-cols-2 sm:grid-cols-3 gap-3` (mb-4 when followed by pills). Pill row: `flex flex-wrap gap-2`.
filter-Boolean scalar idiom: `overviewCells` lines 28-46 — `[cond && {label,value}, ...].filter(Boolean)`.

DELETE the entire Permaculture `<section>` block (lines 150-169). Notable Cultivars (lines 170-176) stays AFTER the four new sections. New sections slot between Landscaping (ends line ~148) and Notable Cultivars.

From lib/plant-labels.ts (Plan 02-01): MONTH_OPTIONS (calendar order — for harvest_months sort), and the 4 InfoCell label consts (forest_garden_layer->`Forest Garden Layer`, establishment_difficulty->`Establishment`, maintenance_level->`Maintenance`, years_to_bearing->`Years to Bearing`).

<!-- tests/plants.spec.ts existing patterns -->
- `test.describe('Plant detail page — public', ...)` starts ~line 247
- Existing detail tests navigate via `page.locator('a[href^="/plants/"]').first().click()` or `page.goto(\`/plants/${id}\`)`
- Test plant ids available: tests/.test-plant-ids.json -> `cultivarsPlantId` / `invasivePlantId` (= `7a36f110-be67-4628-a209-fcc852f6fd91`)
- Notable Cultivars assertion precedent line ~283-289: `page.locator('section').filter({ hasText: 'Notable Cultivars' }).locator('p')`
- CLAUDE.md Playwright gotchas: scope to `section`/`nav` (footer link ambiguity); pills are `rounded-full`; cards `.bg-cream`; tests hit PRODUCTION URL (read-only — safe, this phase adds no mutations)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Permaculture section with 4 dedicated sections</name>
  <files>app/(app)/plants/[id]/page.tsx</files>
  <read_first>
    - app/(app)/plants/[id]/page.tsx (full — InfoCell lines 9-16, overviewCells filter-Boolean lines 28-46, Permaculture block to DELETE lines 150-169, Notable Cultivars 170-176)
    - lib/plant-labels.ts (MONTH_OPTIONS + 4 InfoCell label consts from Plan 02-01)
    - .planning/phases/02-functional-data-enrichment/02-UI-SPEC.md (full — section order lines 94-103, InfoCell labels 105-114, conditional-hide matrix 116-136, pill ordering 133-136)
    - .planning/phases/02-functional-data-enrichment/02-CONTEXT.md (D-05/D-06/D-07 presentation; Specifics lines 118-122)
    - .planning/phases/02-functional-data-enrichment/02-PATTERNS.md (lines 233-276 — detail-page restructure spec, Pitfall 4 years_to_bearing != null)
  </read_first>
  <action>
  In `app/(app)/plants/[id]/page.tsx`: import `MONTH_OPTIONS` and the 4 InfoCell label constants from `@/lib/plant-labels`. D-05: DELETE the entire generic Permaculture `<section>` (lines 150-169) and redistribute its content into the four new sections. Do NOT modify the Supabase query (`.select('*')` already returns the new columns). Insert four new sections in this exact order between Landscaping and Notable Cultivars (D-05, UI-SPEC lines 94-103). D-06: render array/tag fields as the byte-identical pill spans (`text-xs bg-terracotta/10 text-terracotta px-3 py-1.5 rounded-full border border-terracotta/20 capitalize`) and scalar fields as the reused `InfoCell` component — do NOT "correct" `py-1.5` to 8px (UI-SPEC advisory). D-07: each section uses `<section className="mb-8">` + the `h2` eyebrow class string `text-[11px] font-semibold text-warm-stone uppercase tracking-[0.06em] mb-4`, slots after Landscaping and before Notable Cultivars, and conditionally hides null/empty fields via `&&`/filter-Boolean guards.

  1. Functional Roles — `permaculture_uses` pills only (D-06 array→pills). Section guard: `plant.permaculture_uses?.length` (D-07 conditional-hide). Pill row `flex flex-wrap gap-2`, stored order.
  2. Forest Layer & Succession — `forest_garden_layer` InfoCell (D-06 scalar→InfoCell, label `Forest Garden Layer`) + `succession_role` pills (D-06 array→pills). Section guard: `plant.forest_garden_layer || plant.succession_role?.length`. Omit only the layer InfoCell if `forest_garden_layer` null; omit only the succession pill row if `succession_role` empty/null (D-07 per-field, UI-SPEC 124-125). InfoCell grid `grid grid-cols-2 sm:grid-cols-3 gap-3 mb-4` before pills.
  3. Establishment & Care — InfoCells (D-06 scalar→InfoCell) for `establishment_difficulty` (label `Establishment`), `maintenance_level` (label `Maintenance`), `years_to_bearing` (label `Years to Bearing`, value `${n} years`) built via the filter-Boolean idiom — use `plant.years_to_bearing != null` NOT truthiness (Pitfall 4 — 0 falsy bug) — plus `propagation_methods` pills (D-06 array→pills). Section guard: any of the four present (D-07). Omit individual InfoCells that are null; omit pill row if `propagation_methods` empty/null.
  4. Harvest — `edible_parts` pills (stored order) + `harvest_months` pills sorted by `MONTH_OPTIONS` calendar order (D-06 array→pills; UI-SPEC 135 — sort harvest_months by index in MONTH_OPTIONS, identical to bloom_months treatment; all OTHER arrays stay in stored order). Section guard: `plant.edible_parts?.length || plant.harvest_months?.length` (D-07). Omit only the edible-parts pill row if empty; omit only the harvest-months pill row if empty (D-07, UI-SPEC 129-131).

  D-07: no empty-state copy anywhere — missing data is hidden via `&&`/filter-Boolean guards, never labeled N/A or Unknown (UI-SPEC 116-118). If a small `<TagPills label value[] />` helper reduces repetition it MUST emit byte-identical markup/classes (D-06; UI-SPEC Component Inventory note).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Functional Roles" "app/(app)/plants/[id]/page.tsx" && grep -q "Forest Layer & Succession" "app/(app)/plants/[id]/page.tsx" && grep -q "Establishment & Care" "app/(app)/plants/[id]/page.tsx" && grep -q "Harvest" "app/(app)/plants/[id]/page.tsx" && grep -q "MONTH_OPTIONS" "app/(app)/plants/[id]/page.tsx" && grep -q "years_to_bearing != null" "app/(app)/plants/[id]/page.tsx" && ! grep -q ">Permaculture<" "app/(app)/plants/[id]/page.tsx" && grep -q "bg-terracotta/10 text-terracotta px-3 py-1.5 rounded-full border border-terracotta/20 capitalize" "app/(app)/plants/[id]/page.tsx" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0
    - D-05: page contains the 4 section eyebrow titles in order: `Functional Roles`, `Forest Layer & Succession`, `Establishment & Care`, `Harvest`; old standalone `>Permaculture<` `<h2>` no longer present (grep returns nothing)
    - D-06: pill markup string `bg-terracotta/10 text-terracotta px-3 py-1.5 rounded-full border border-terracotta/20 capitalize` present for array fields (byte-identical reuse; `py-1.5` retained); scalar fields use the reused `InfoCell`
    - `years_to_bearing != null` guard present (not truthiness — Pitfall 4)
    - `MONTH_OPTIONS` imported and used to order harvest_months
    - Supabase query unchanged (still `.select('*')` — grep `.select('*')` still present, no `.select('id,` field list added)
    - D-07: 4 InfoCell label constants imported from `@/lib/plant-labels` (not inlined string literals in JSX for the labels); section/field-level `&&` conditional-hide guards present
  </acceptance_criteria>
  <done>D-05/D-06/D-07: detail page renders 4 conditionally-hidden sections (pills for arrays, InfoCells for scalars) with byte-identical reused markup; generic Permaculture section retired/redistributed; build/tsc/lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright assertions for the 4 new sections + conditional-hide</name>
  <files>tests/plants.spec.ts</files>
  <read_first>
    - tests/plants.spec.ts (full — `Plant detail page — public` describe ~line 247; Notable Cultivars precedent ~283-289; navigation patterns)
    - tests/.test-plant-ids.json (cultivarsPlantId / invasivePlantId for direct goto)
    - playwright.config.ts (targets production URL)
    - .planning/phases/02-functional-data-enrichment/02-UI-SPEC.md (lines 116-136 — conditional-hide matrix the tests assert)
    - CLAUDE.md (Playwright gotchas — scope to section, pills rounded-full, cards .bg-cream, footer link ambiguity)
  </read_first>
  <action>Extend the `test.describe('Plant detail page — public', ...)` block in `tests/plants.spec.ts` with new tests for the 4 sections (D-05). Scope all assertions to `page.locator('section').filter({ hasText: '<Section Title>' })` (mirrors the existing Notable Cultivars assertion pattern; avoids footer link ambiguity per CLAUDE.md). Add: (a) a test that navigates to a known enriched plant (use `.test-plant-ids.json` id via `page.goto(\`/plants/${id}\`)` or the first plant card) and asserts the `Functional Roles` section is present with at least one pill (`section ... .locator('span').first()` visible, D-06 pill class `rounded-full`); (b) a test asserting the `Forest Layer & Succession` section renders (layer InfoCell label `Forest Garden Layer` and/or succession pills); (c) a test asserting `Establishment & Care` shows at least one InfoCell label among `Establishment`/`Maintenance`/`Years to Bearing` and/or propagation pills; (d) a test asserting the `Harvest` section either renders edible/harvest pills OR is absent (D-07 conditional-hide — assert `expect(section).toHaveCount(0)` for a known non-edible plant if one is identified, else assert presence on an edible plant); (e) a conditional-hide regression: assert the old `Permaculture` section heading is NOT present (D-05 — `await expect(page.locator('section h2', { hasText: /^Permaculture$/ })).toHaveCount(0)`). Keep tests read-only (no mutations — production-safe per CLAUDE.md). Do not introduce bare `page.getByRole` for links (footer ambiguity).</action>
  <verify>
    <automated>npx playwright test tests/plants.spec.ts --grep "detail" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/plants.spec.ts --grep "detail"` passes (all detail-page tests green, including the new section tests)
    - D-05: new tests assert each of the 4 section titles scoped to `page.locator('section').filter(...)` (not bare getByRole/getByText)
    - D-05: a regression test asserts the old `Permaculture` standalone section heading has count 0
    - D-07: conditional-hide behavior covered for at least the Harvest section (present-when-data / absent-when-empty)
    - No mutations performed (read-only navigation only)
  </acceptance_criteria>
  <done>D-05/D-07: Playwright detail-page suite green with new assertions covering the 4 sections + conditional-hide + Permaculture-removed regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase `plants` row -> RSC render | DB-stored (already vocabulary-validated) values rendered as React text |
| (no user-input boundary) | Read-only display phase — no forms, mutations, or new routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-XSS | Tampering / XSS | detail-page render of enriched values | accept | Values are vocabulary-constrained enums/arrays (validated at write time in Plan 02-02, V5); rendered as React text children (auto-escaped); no `dangerouslySetInnerHTML` introduced — no new surface (RESEARCH Security Domain) |
| T-02-04 | Access Control | new columns on detail page | accept | Detail page already public; new columns inherit `plants` RLS SELECT `USING (true)` (public catalog, intended); no auth/session surface added |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` exit 0
- `npx playwright test tests/plants.spec.ts --grep "detail"` green
- Permaculture section removed; 4 new sections render with conditional-hide; pill markup byte-identical (UI-SPEC reuse-only)
- Supabase query unchanged (`.select('*')`)
</verification>

<success_criteria>
- Detail page shows functional roles, forest layer + succession, establishment + care, harvest (DATA-01..DATA-04 user-facing)
- Empty/null fields hidden, never labeled N/A; harvest_months in calendar order
- Playwright suite green including conditional-hide + Permaculture-removed regression
</success_criteria>

<output>
Create `.planning/phases/02-functional-data-enrichment/02-03-SUMMARY.md` when done
</output>
</output>
