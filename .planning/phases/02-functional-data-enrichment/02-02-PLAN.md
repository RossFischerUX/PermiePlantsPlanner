---
phase: 02-functional-data-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/enrich-functional-data.ts
  - package.json
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "Running the enrichment script populates all 7 new fields + re-normalizes permaculture_uses for the ~250-plant catalog"
    - "permaculture_uses is ALWAYS overwritten to the D-02 16-tag vocab (re-enrich, never skipped)"
    - "The 7 new fields are skip-if-populated per-field (a second run does no work for already-filled rows)"
    - "`npm run enrich-functional-data -- --verify` prints per-field coverage and exits non-zero if any required field is null"
  artifacts:
    - path: "scripts/enrich-functional-data.ts"
      provides: "Claude Haiku enrichment with two opposite skip paths + --verify branch"
      min_lines: 150
      contains: "FUNCTIONAL_ROLE"
    - path: "package.json"
      provides: "enrich-functional-data npm script"
      contains: "enrich-functional-data"
  key_links:
    - from: "scripts/enrich-functional-data.ts"
      to: "lib/plant-labels.ts vocab constants"
      via: "import FUNCTIONAL_ROLE_OPTIONS / SUCCESSION_OPTIONS / etc. for validation Sets"
      pattern: "FUNCTIONAL_ROLE_OPTIONS|SUCCESSION_OPTIONS|PROPAGATION_OPTIONS|EDIBLE_PART_OPTIONS"
    - from: "scripts/enrich-functional-data.ts targeting query"
      to: "plants table"
      via: ".or() OR-of-nulls on the 7 NEW fields ONLY (permaculture_uses excluded — D-01 re-enrich)"
      pattern: "\\.or\\("
---

<objective>
Build the dedicated enrichment pipeline `scripts/enrich-functional-data.ts` that (1) ALWAYS re-enriches/overwrites `permaculture_uses` to the D-02 16-tag vocab on every targeted row (D-01), (2) backfills the 7 NEW fields with per-field skip-if-populated semantics (D-17), and (3) provides a `--verify` read-only coverage gate (D-18/D-19). Then run it against the live catalog and confirm the verify gate passes.

Purpose: This is the data slice — after this plan the catalog actually holds structured functional data, and DATA-05's post-run verification gate exists and is green. The two opposite skip semantics in one script are the single most error-prone divergence (PATTERNS Pitfall 2) and are encoded as separate code paths with separate acceptance criteria.
Output: New `scripts/enrich-functional-data.ts`, `package.json` script entry, enriched live data, passing `--verify`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-functional-data-enrichment/02-CONTEXT.md
@.planning/phases/02-functional-data-enrichment/02-RESEARCH.md
@.planning/phases/02-functional-data-enrichment/02-PATTERNS.md
@.planning/phases/02-functional-data-enrichment/02-01-SUMMARY.md

<interfaces>
<!-- From lib/plant-labels.ts (created by Plan 02-01). Build validation Sets from these. -->
FUNCTIONAL_ROLE_OPTIONS (16 D-02 tags), SUCCESSION_OPTIONS (D-09: pioneer|early successional|mid successional|climax), ESTABLISHMENT_OPTIONS (D-10: easy|moderate|challenging), MAINTENANCE_OPTIONS (D-11: low|moderate|high), PROPAGATION_OPTIONS (D-13: seed|cutting|division|layering|grafting|root cutting|tuber|sucker), EDIBLE_PART_OPTIONS (D-14: leaf|fruit|nut|seed|root|flower|bark|sap|shoot|pod), MONTH_OPTIONS (12 month names — reuse for harvest_months validation/format, D-15).

<!-- Reference scaffold scripts/update-existing-plants.ts (copy-adapt verbatim where noted) -->
- Imports/client/config/sleep: lines 18-32 + service-role client + `CLAUDE_BATCH_SIZE=10`, `CLAUDE_BATCH_DELAY_MS=15000` (CLAUDE.md HARD RULE — never shorten)
- Env guards lines 149-150: throw if SERVICE_ROLE_KEY / ANTHROPIC_API_KEY missing
- `normalizeEnum<T>(value, allowed): T|null` lines 91-94 — COPY VERBATIM (single-enum drop-invalid)
- Claude JSON-in-text idiom lines 96-141: `anthropic.messages.create({ model: 'claude-haiku-4-5-20251001', max_tokens })` -> `response.content[0].text` -> `text.match(/\{[\s\S]*\}/)` -> `JSON.parse` in try/catch returning null. RAISE max_tokens 600 to ~900 (Pitfall 5, larger schema)
- Batch loop lines 177-230: `for (i; i+=CLAUDE_BATCH_SIZE) { Promise.all(batch.map(...)); if (processed < len) await sleep(CLAUDE_BATCH_DELAY_MS) }` — COPY STRUCTURE
- Fatal tail lines 238-241: `main().catch(err => { console.error('Fatal error:', err); process.exit(1) })`

<!-- Array-vocab validator adapted from scripts/backfill-native-states.ts:77 (Set .filter drop-invalid, D-03) -->
`const validArr = (arr, allow: Set<string>) => Array.isArray(arr) ? arr.filter((s): s is string => typeof s === 'string' && allow.has(s.toLowerCase())) : []`

<!-- package.json scripts — add after "update-plants" (line 11) -->
"enrich-functional-data": "tsx scripts/enrich-functional-data.ts"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enrichment script with two opposite skip paths</name>
  <files>scripts/enrich-functional-data.ts, package.json</files>
  <read_first>
    - scripts/update-existing-plants.ts (full — copy scaffold: imports/config/env-guard/normalizeEnum/Claude-idiom/batch-loop/fatal-tail)
    - scripts/backfill-native-states.ts (lines 7-9, 77, 114 — Set-filter array validator + empty-array-not-null idempotency convention)
    - scripts/backfill-usda-zones.ts (lines 118-123 — mixed not-null/null targeting precedent)
    - lib/plant-labels.ts (vocab constants created by Plan 02-01)
    - .planning/phases/02-functional-data-enrichment/02-CONTEXT.md (D-01 always-overwrite roles; D-16 new dedicated script; D-17 per-field skip)
    - .planning/phases/02-functional-data-enrichment/02-PATTERNS.md (lines 23-141 — full script pattern incl. per-field merge code; Pitfall 2 + Pitfall 3)
    - .planning/phases/02-functional-data-enrichment/02-RESEARCH.md (lines 155-219, 327-358 — Pattern 1 targeting, Pitfalls 1-3, 5)
  </read_first>
  <action>
  Create `scripts/enrich-functional-data.ts` by copying the scaffold from `update-existing-plants.ts` (imports, dotenv, service-role client `createClient(SUPABASE_URL, SUPABASE_SECRET_KEY ?? SUPABASE_SERVICE_ROLE_KEY)`, `CLAUDE_BATCH_SIZE=10`, `CLAUDE_BATCH_DELAY_MS=15000` verbatim — CLAUDE.md hard rule, never shorten, never remove the 15s sleep, never "upgrade" the model string `claude-haiku-4-5-20251001`). Import the vocab constants from `lib/plant-labels` (matching the relative-import style the other scripts use) and build `Set` instances for array validation; copy `normalizeEnum` verbatim for single enums; adapt the `validArr(arr, allow: Set)` Set-filter from `backfill-native-states.ts:77` (D-03 drop-invalid).

  Define a `FunctionalData` TypeScript interface for the 8 Claude-returned fields. Claude prompt: request a single JSON object with `permaculture_uses` (D-02 16-tag), `succession_role` (D-09), `establishment_difficulty`/`maintenance_level` (D-10/D-11), `years_to_bearing` (integer|null), `propagation_methods` (D-13), `edible_parts` (D-14), `harvest_months` (MONTH_OPTIONS names matching `bloom_months` format, D-15). Use JSON-in-text extraction (`/\{[\s\S]*\}/` -> `JSON.parse` in try/catch -> null). Set `max_tokens: 900` (raised from 600 — Pitfall 5).

  TWO OPPOSITE SKIP PATHS (PATTERNS Pitfall 2 — the single most error-prone divergence; encode as visibly separate code):
  - Targeting query: `TARGET_FIELDS = ['succession_role','establishment_difficulty','maintenance_level','years_to_bearing','propagation_methods','edible_parts','harvest_months']`. Select rows with `.or(TARGET_FIELDS.map(f => \`${f}.is.null\`).join(','))` — OR-of-nulls on the 7 NEW fields ONLY. `permaculture_uses` is DELIBERATELY NOT in TARGET_FIELDS and NOT in the `.or()` clause (D-01: re-enrich/overwrite ALL targeted rows). Also select `id, common_name, latin_name, permaculture_uses` + the 7 fields. Order by `common_name`.
  - Per-field merge update: ALWAYS set `update.permaculture_uses = validArr(raw.permaculture_uses, FUNCTIONAL_ROLE_SET)` unconditionally (D-01 overwrite). Then per-field guard ONLY the 7 new fields: `if (row.succession_role == null) update.succession_role = validArr(...)`; `if (row.establishment_difficulty == null) update.establishment_difficulty = normalizeEnum(...)`; `if (row.maintenance_level == null) update.maintenance_level = normalizeEnum(...)`; `if (row.years_to_bearing == null) update.years_to_bearing = Number.isInteger(raw.years_to_bearing) ? raw.years_to_bearing : null`; `if (row.propagation_methods == null) update.propagation_methods = validArr(...)`; `if (row.edible_parts == null) update.edible_parts = validArr(...)` (writes `[]` for genuinely non-edible — deliberate, idempotent per backfill-native-states convention; NULL kept only on Claude failure so partial-failure reruns retry — Pitfall 3); `if (row.harvest_months == null) update.harvest_months = validArr(...)`. Apply via `.update(update).eq('id', row.id)` only when `Object.keys(update).length > 0` (permaculture_uses always present so it always runs — correct).

  Add a comment block near the targeting query explicitly stating: "permaculture_uses = ALWAYS overwrite (D-01); the 7 new fields = per-field skip-if-populated (D-17). These are intentionally opposite — see PATTERNS Pitfall 2." Copy the batch loop + 15s sleep + fatal-error tail verbatim. Add `"enrich-functional-data": "tsx scripts/enrich-functional-data.ts"` to `package.json` scripts after `"update-plants"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f scripts/enrich-functional-data.ts && grep -q "claude-haiku-4-5-20251001" scripts/enrich-functional-data.ts && grep -q "CLAUDE_BATCH_DELAY_MS = 15000" scripts/enrich-functional-data.ts && grep -q "max_tokens: 900" scripts/enrich-functional-data.ts && grep -E "TARGET_FIELDS *=" scripts/enrich-functional-data.ts && ! grep -E "TARGET_FIELDS[^=]*=.*permaculture_uses" scripts/enrich-functional-data.ts && grep -q '"enrich-functional-data": "tsx scripts/enrich-functional-data.ts"' package.json && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
    - `scripts/enrich-functional-data.ts` exists, >=150 lines
    - Model literal is exactly `claude-haiku-4-5-20251001`; `CLAUDE_BATCH_SIZE = 10`; `CLAUDE_BATCH_DELAY_MS = 15000` (all three grep succeed); 15s `sleep` present in batch loop
    - `max_tokens: 900` present (raised per Pitfall 5)
    - `TARGET_FIELDS` array exists and does NOT include the string `permaculture_uses` (grep `TARGET_FIELDS=...permaculture_uses` returns nothing)
    - `.or(` OR-of-nulls targeting present; `update.permaculture_uses` assigned unconditionally (not behind an `if (row... == null)` guard); each of the 7 new fields assigned only behind an `if (row.<field> == null)` guard
    - Comment block explicitly documenting the two opposite skip semantics present
    - `package.json` contains `"enrich-functional-data": "tsx scripts/enrich-functional-data.ts"`
  </acceptance_criteria>
  <done>Script compiles, rate-limit + model verbatim, two opposite skip paths encoded with permaculture_uses excluded from TARGET_FIELDS and always-written, npm script added.</done>
</task>

<task type="auto">
  <name>Task 2: Add --verify read-only coverage gate (DATA-05)</name>
  <files>scripts/enrich-functional-data.ts</files>
  <read_first>
    - scripts/enrich-functional-data.ts (the file from Task 1)
    - .planning/phases/02-functional-data-enrichment/02-CONTEXT.md (D-18 verify subcommand; D-19 enriched set + exemptions; Specifics line-format `functional_roles (permaculture_uses): 248/250`)
    - .planning/phases/02-functional-data-enrichment/02-PATTERNS.md (lines 144-167 — --verify branch design, NO repo analog)
    - .planning/phases/02-functional-data-enrichment/02-RESEARCH.md (lines 221-247 — verify pattern, exit codes)
  </read_first>
  <action>Add a `verify()` async function and branch on `process.argv.includes('--verify')` (npm passes flags after `--`, so `npm run enrich-functional-data -- --verify` reaches `process.argv`). `verify()` is READ-ONLY (no Claude calls, no UPDATE). Compute `total` = `supabase.from('plants').select('id', { count: 'exact', head: true })`. `REQUIRED = ['permaculture_uses','succession_role','establishment_difficulty','maintenance_level','propagation_methods']`. For each required field, count `.not(f,'is',null)`; print `  ${label}: ${ok}/${total} ${ok===total?'OK':'FAIL'}` using the CONTEXT line-format style (for permaculture_uses use the label `functional_roles (permaculture_uses)`). On any shortfall set `failed=true`, query `.is(f,null)` and print each offending `common_name (id)`. `years_to_bearing` is EXEMPT from the non-null assertion (D-19 — legitimately null for non-food plants). `edible_parts`/`harvest_months` are NOT in REQUIRED — assert only column-typed-presence (a `.select('edible_parts, harvest_months').limit(1)` succeeding proves the typed columns exist; empty `{}` arrays are valid per D-19). End with `process.exit(failed ? 1 : 0)`. Branch at the entrypoint: `if (process.argv.includes('--verify')) { await verify() } else { await main() }` keeping the existing fatal-error `.catch` wrapper.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "process.argv.includes('--verify')" scripts/enrich-functional-data.ts && grep -qE "REQUIRED *=.*permaculture_uses.*succession_role.*establishment_difficulty.*maintenance_level.*propagation_methods" scripts/enrich-functional-data.ts && ! grep -E "REQUIRED[^=]*=.*years_to_bearing" scripts/enrich-functional-data.ts && grep -q "process.exit(failed ? 1 : 0)" scripts/enrich-functional-data.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0
    - `process.argv.includes('--verify')` branch present at entrypoint
    - `REQUIRED` array = exactly `permaculture_uses, succession_role, establishment_difficulty, maintenance_level, propagation_methods` (ordered grep succeeds)
    - `years_to_bearing` NOT in `REQUIRED` (grep `REQUIRED=...years_to_bearing` returns nothing — D-19 exemption)
    - `edible_parts`/`harvest_months` NOT in `REQUIRED` (typed-presence only per D-19)
    - `process.exit(failed ? 1 : 0)` present (non-zero on coverage gap)
    - `verify()` performs NO `anthropic.messages.create` and NO `.update(` calls (read-only)
  </acceptance_criteria>
  <done>`--verify` is read-only, prints per-field coverage in CONTEXT line format, exits non-zero listing offending ids on any required-field gap, exempts years_to_bearing + edible/harvest per D-19.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run enrichment pipeline + confirm verify gate (DATA-05)</name>
  <what-built>The enrichment script (Task 1) + `--verify` gate (Task 2). This task executes them against the live ~250-plant catalog.</what-built>
  <action>Requires Plan 02-01 migration applied to the live DB and `.env.local` containing `ANTHROPIC_API_KEY` + `SUPABASE_SECRET_KEY`/`SUPABASE_SERVICE_ROLE_KEY`. Run `npm run enrich-functional-data` (full catalog — ~250 plants at 10/15s ~ 6-7 min; the 15s batch delay is mandatory and must not be removed). Then run `npm run enrich-functional-data -- --verify` and confirm it exits 0 with all required fields at full coverage. Then run `npm run enrich-functional-data` a SECOND time to prove per-field skip-if-populated: once all 7 new fields are populated, the OR-of-nulls target query returns 0 rows so the second run is a true no-op (correct DATA-05 behavior — permaculture_uses is only re-written for rows the `.or()` target returns, which is now empty). Spot-check ~10 plants whose legacy permaculture_uses had free-form values to confirm semantic normalization (manual-only per VALIDATION.md — e.g. legacy `pioneer` now appears in succession_role, not roles).</action>
  <how-to-verify>
    1. `npm run enrich-functional-data` completes without a fatal error (final summary prints updated/skipped/failed counts).
    2. `npm run enrich-functional-data -- --verify` prints per-field `field: N/total` lines and exits 0 (`echo $?` -> 0).
    3. Re-run `npm run enrich-functional-data`: target query returns 0 rows / no per-field work for already-filled rows (skip-if-populated proven).
    4. In Supabase dashboard, spot-check ~10 plants: `permaculture_uses` values all within the D-02 16-tag vocab; legacy tags like `pioneer` migrated to `succession_role`; no role-vs-succession category errors.
  </how-to-verify>
  <verify>
    <automated>npm run enrich-functional-data -- --verify</automated>
  </verify>
  <acceptance_criteria>
    - `npm run enrich-functional-data -- --verify` exits 0 (all REQUIRED fields full coverage)
    - First enrichment run completed without fatal error
    - Second enrichment run is a no-op for populated rows (skip-if-populated / "Nothing to do." — DATA-05)
    - Manual spot-check: ~10 plants confirm permaculture_uses conforms to D-02 vocab with no role/succession category errors
  </acceptance_criteria>
  <resume-signal>Type "verified" once `--verify` exits 0 and the rerun no-op + spot-check are confirmed, or describe the failing fields/ids.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Haiku response -> `plants` TEXT[]/enum columns | Untrusted AI-generated strings cross into the DB here |
| `.env.local` secrets -> service-role Supabase client | Service-role key (RLS bypass) used for writes |
| process args -> `--verify` branch | CLI flag selects read-only vs. write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-V5 | Tampering | Claude output -> `plants` columns | mitigate | V5 input validation (RESEARCH Security Domain): every field validated before UPDATE — single enums via `normalizeEnum` (drop-invalid), arrays via `validArr` Set `.filter()` against fixed D-02/D-09/D-13/D-14/MONTH vocab Sets (D-03 drop-invalid). Arbitrary AI strings cannot enter TEXT[]/enum columns. |
| T-02-ID | Information Disclosure | service-role key | accept | Key is scripts-only, never client-side (CLAUDE.md); `.env.local` not committed; unchanged from existing 4 enrichment scripts (RESEARCH Security Domain) |
| T-02-DOS | Denial of Service | Claude API rate limit | mitigate | `CLAUDE_BATCH_SIZE=10` + `CLAUDE_BATCH_DELAY_MS=15000` copied verbatim (CLAUDE.md hard rule — 40 req/min, under the 50/min haiku limit); never shortened |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — phase installs ZERO external packages (RESEARCH Package Legitimacy Audit: not applicable, all deps pinned in lockfile v3); no slopcheck checkpoint needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run lint` exits 0 (per task)
- `npm run enrich-functional-data -- --verify` exits 0 (phase data gate — DATA-05)
- Second enrichment run is a no-op for populated rows (DATA-05 skip-if-populated)
- `permaculture_uses` always-overwrite path NOT behind a skip guard; 7 new fields per-field guarded; `permaculture_uses` NOT in `TARGET_FIELDS`
- Manual spot-check confirms semantic normalization (VALIDATION.md manual-only row)
</verification>

<success_criteria>
- All 7 new fields + re-normalized `permaculture_uses` populated across the ~250-plant catalog
- `--verify` gate exists, prints per-field coverage, exits non-zero on gaps (DATA-05)
- Rerun safety proven (per-field skip-if-populated; permaculture_uses always-overwrite)
- Claude output whitelist-validated before every DB write (V5 / D-03)
</success_criteria>

<output>
Create `.planning/phases/02-functional-data-enrichment/02-02-SUMMARY.md` when done
</output>
