---
phase: 02-functional-data-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260519XXXXXX_add_functional_data_fields.sql
  - lib/types.ts
  - lib/plant-labels.ts
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "The plants table has 7 new functional-data columns in the live Supabase DB"
    - "The Plant TypeScript interface compiles with the 7 new fields under strict mode"
    - "lib/plant-labels.ts exports the D-02 16-tag functional-role vocabulary and the v1 filter sidebar still compiles against it"
    - "D-08: ForestGardenLayer type/forest_garden_layer column left unchanged (not re-added)"
    - "D-09: succession_role is TEXT[] constrained to vocab pioneer|early successional|mid successional|climax"
    - "D-10: establishment_difficulty is TEXT CHECK easy|moderate|challenging"
    - "D-11: maintenance_level is TEXT CHECK low|moderate|high"
    - "D-12: years_to_bearing is INTEGER nullable"
    - "D-13: propagation_methods is TEXT[] constrained to the propagation vocab"
    - "D-14: edible_parts is TEXT[] vocab; empty array means not edible (not null)"
    - "D-15: harvest_months is TEXT[] using the same month-name format as bloom_months"
    - "D-04: no new filter UI added — schema/display/pipeline only; PERM_USE_OPTIONS export preserved for existing importers, not new filter controls"
  artifacts:
    - path: "supabase/migrations/20260519XXXXXX_add_functional_data_fields.sql"
      provides: "ALTER TABLE adding succession_role, propagation_methods, edible_parts, harvest_months (TEXT[]), establishment_difficulty/maintenance_level (TEXT CHECK), years_to_bearing (INTEGER)"
      contains: "ADD COLUMN succession_role"
    - path: "lib/types.ts"
      provides: "7 new fields on Plant interface"
      contains: "succession_role: string[] | null"
    - path: "lib/plant-labels.ts"
      provides: "FUNCTIONAL_ROLE_OPTIONS (16 tags) + 5 new vocab consts + 4 InfoCell label consts"
      contains: "FUNCTIONAL_ROLE_OPTIONS"
  key_links:
    - from: "lib/types.ts Plant interface"
      to: "migration column names"
      via: "exact name match (succession_role, establishment_difficulty, maintenance_level, years_to_bearing, propagation_methods, edible_parts, harvest_months)"
      pattern: "succession_role|establishment_difficulty|maintenance_level|years_to_bearing|propagation_methods|edible_parts|harvest_months"
    - from: "app/(app)/plants/FilterControls.tsx"
      to: "lib/plant-labels.ts PERM_USE_OPTIONS"
      via: "import (live v1 filter sidebar consumer — must still resolve)"
      pattern: "PERM_USE_OPTIONS"
---

<objective>
Lay the schema + type + vocabulary foundation for functional data enrichment: add 7 columns to the live `plants` table, extend the `Plant` interface, and reconcile the conflicting legacy `PERM_USE_OPTIONS` vocabulary with the D-02 16-tag controlled vocabulary.

Purpose: Every downstream slice (enrichment script writes, detail-page reads) depends on these columns existing in the live DB and the typed contract matching. The `PERM_USE_OPTIONS` reconciliation resolves the HIGH-severity data hazard (RESEARCH Pitfall 1) before any data is written.
Output: One new migration applied via `supabase db push`, updated `lib/types.ts`, reconciled + extended `lib/plant-labels.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-functional-data-enrichment/02-CONTEXT.md
@.planning/phases/02-functional-data-enrichment/02-RESEARCH.md
@.planning/phases/02-functional-data-enrichment/02-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

Current lib/types.ts Plant interface (insert new fields after `permaculture_uses: string[] | null` line 31, before `notable_cultivars: string | null` line 32). forest_garden_layer (line 30) and permaculture_uses (line 31) ALREADY EXIST — do NOT re-add:
```typescript
export type ForestGardenLayer = 'canopy' | 'sub-canopy' | 'shrub' | 'herb' | 'ground cover' | 'rhizosphere' | 'climber'
// ...
  forest_garden_layer: ForestGardenLayer | null   // line 30 — exists
  permaculture_uses: string[] | null               // line 31 — exists
  notable_cultivars: string | null                 // line 32 — exists
```

Current lib/plant-labels.ts (line 9 is the LEGACY 10-tag vocab that CONFLICTS with D-02; line 4 MONTH_OPTIONS already exists and is reused for harvest):
```typescript
export const MONTH_OPTIONS: string[] = ['January','February','March','April','May','June','July','August','September','October','November','December']  // line 4 — reuse
export const PERM_USE_OPTIONS: string[] = ['nitrogen fixer','dynamic accumulator','edible','medicinal','pollinator','biomass','windbreak','wildlife habitat','pioneer','insectary']  // line 9 — LEGACY, must be reconciled
```

Migration analog supabase/migrations/20260515024612_add_permaculture_fields.sql (mirror this exact style — TEXT CHECK for single enums, plain TEXT[] for arrays, no DB CHECK on arrays):
```sql
ALTER TABLE plants
  ADD COLUMN forest_garden_layer TEXT
    CHECK (forest_garden_layer IN ('canopy', 'sub-canopy', 'shrub', 'herb', 'ground cover', 'rhizosphere', 'climber')),
  ADD COLUMN permaculture_uses TEXT[];
```

LIVE CONSUMERS of PERM_USE_OPTIONS / permUses filter (RESEARCH incorrectly said "none found" — these are real and must keep compiling/working):
- `app/(app)/plants/FilterControls.tsx:7` imports PERM_USE_OPTIONS; `:40` `<FilterSection label="Permaculture Uses" options={PERM_USE_OPTIONS} ...>`
- `app/(app)/plants/queryBuilder.ts:20` `query.contains('permaculture_uses', params.permUses)`
- `app/(app)/plants/ActiveFilterChips.tsx`, `PlantsFilterSidebar.tsx`, `searchParams.ts:11` — `permUses` filter state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write functional-data migration</name>
  <files>supabase/migrations/20260519XXXXXX_add_functional_data_fields.sql</files>
  <read_first>
    - supabase/migrations/20260515024612_add_permaculture_fields.sql (exact mirror style — CHECK enum + plain TEXT[])
    - supabase/migrations/20260515024611_add_plant_detail_fields.sql (plain ADD COLUMN precedent)
    - .planning/phases/02-functional-data-enrichment/02-CONTEXT.md (D-08..D-15 field shapes; D-discretion: no DB CHECK on arrays)
    - .planning/phases/02-functional-data-enrichment/02-PATTERNS.md (lines 171-189 — migration pattern + anti-patterns)
  </read_first>
  <action>Create `supabase/migrations/{14-digit-timestamp}_add_functional_data_fields.sql`. Use a real timestamp generated at execution time following the `supabase/migrations/` 14-digit convention (e.g. `date +%Y%m%d%H%M%S`); rename the placeholder `20260519XXXXXX` accordingly and update this plan's `files_modified` mention only conceptually (the actual filename uses the generated timestamp). One `ALTER TABLE plants` statement adding exactly: `succession_role TEXT[]`, `propagation_methods TEXT[]`, `edible_parts TEXT[]`, `harvest_months TEXT[]` (all plain arrays, NO DB CHECK per D-discretion + permaculture_uses precedent), `establishment_difficulty TEXT CHECK (establishment_difficulty IN ('easy','moderate','challenging'))` (D-10), `maintenance_level TEXT CHECK (maintenance_level IN ('low','moderate','high'))` (D-11), `years_to_bearing INTEGER` (D-12). Do NOT re-add `forest_garden_layer` or `permaculture_uses` — they already exist in migration 20260515024612 and re-adding errors (PATTERNS anti-pattern). Add a header comment mirroring migration 612's comment style.</action>
  <verify>
    <automated>test -f supabase/migrations/*_add_functional_data_fields.sql && grep -q "ADD COLUMN succession_role TEXT\[\]" supabase/migrations/*_add_functional_data_fields.sql && grep -q "establishment_difficulty IN ('easy','moderate','challenging')" supabase/migrations/*_add_functional_data_fields.sql && ! grep -q "ADD COLUMN forest_garden_layer" supabase/migrations/*_add_functional_data_fields.sql && ! grep -q "ADD COLUMN permaculture_uses" supabase/migrations/*_add_functional_data_fields.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists matching `supabase/migrations/*_add_functional_data_fields.sql` with a real 14-digit timestamp prefix
    - Contains `ADD COLUMN succession_role TEXT[]`, `ADD COLUMN propagation_methods TEXT[]`, `ADD COLUMN edible_parts TEXT[]`, `ADD COLUMN harvest_months TEXT[]`
    - Contains `CHECK (establishment_difficulty IN ('easy','moderate','challenging'))` and `CHECK (maintenance_level IN ('low','moderate','high'))`
    - Contains `ADD COLUMN years_to_bearing INTEGER`
    - Does NOT contain `ADD COLUMN forest_garden_layer` or `ADD COLUMN permaculture_uses` (grep returns nothing)
    - No `CHECK` constraint applied to any of the 4 `TEXT[]` columns
  </acceptance_criteria>
  <done>D-09/D-10/D-11/D-12/D-13/D-14/D-15: migration file present, columns/constraints exactly per D-08..D-15 (D-08: forest_garden_layer NOT re-added), no duplicate columns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration to live Supabase DB</name>
  <what-built>Migration file from Task 1 adding 7 functional-data columns to the `plants` table.</what-built>
  <action>Run `supabase db push` to apply the migration to the live Supabase database. This is MANDATORY and BLOCKING: the enrichment script (Plan 02-02) writes to these columns and `--verify` queries them; build/tsc pass WITHOUT the push (TS types come from lib/types.ts, not the live DB) creating a false-positive verification state. If `supabase db push` requires interactive auth, set `SUPABASE_ACCESS_TOKEN` env var; if it still prompts interactively and cannot be suppressed, perform the push manually via the Supabase dashboard SQL editor (CLAUDE.md documents both paths). Do not proceed to Plan 02-02 until columns exist in the live DB.</action>
  <how-to-verify>
    1. Run `supabase db push` (or apply the migration SQL in the Supabase dashboard SQL editor).
    2. Confirm output reports the migration applied with no error.
    3. Optionally verify in Supabase dashboard: Table Editor → `plants` → confirm `succession_role`, `propagation_methods`, `edible_parts`, `harvest_months`, `establishment_difficulty`, `maintenance_level`, `years_to_bearing` columns are present.
  </how-to-verify>
  <verify>
    <automated>MANUAL — schema push verified by human; automated DB introspection requires service-role connection deferred to Plan 02-02 --verify gate</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` (or dashboard apply) completes without error
    - All 7 new columns visible on the `plants` table in the live Supabase DB
  </acceptance_criteria>
  <resume-signal>Type "applied" once the migration is confirmed live in Supabase, or describe the error.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend Plant interface + reconcile and extend plant-labels vocabulary</name>
  <files>lib/types.ts, lib/plant-labels.ts</files>
  <read_first>
    - lib/types.ts (full — Plant interface lines 6-32; insert point after line 31)
    - lib/plant-labels.ts (full — line 9 PERM_USE_OPTIONS conflict, line 4 MONTH_OPTIONS reuse)
    - app/(app)/plants/FilterControls.tsx (lines 7, 40 — live PERM_USE_OPTIONS importer)
    - .planning/phases/02-functional-data-enrichment/02-CONTEXT.md (D-02 16-tag vocab; D-09/D-13/D-14 array vocabs; D-10/D-11 enums)
    - .planning/phases/02-functional-data-enrichment/02-PATTERNS.md (lines 193-229 — types + plant-labels DATA HAZARD section, exact constant arrays)
    - .planning/phases/02-functional-data-enrichment/02-UI-SPEC.md (lines 105-114 — the 4 InfoCell label strings)
  </read_first>
  <action>
  (A) In `lib/types.ts`, add to the `Plant` interface immediately after `permaculture_uses: string[] | null` (line 31) and before `notable_cultivars`: `succession_role: string[] | null`, `establishment_difficulty: string | null`, `maintenance_level: string | null`, `years_to_bearing: number | null`, `propagation_methods: string[] | null`, `edible_parts: string[] | null`, `harvest_months: string[] | null`. Names MUST match the migration column names exactly. Do NOT re-add `forest_garden_layer`/`permaculture_uses`. `ForestGardenLayer` type unchanged (D-08).

  (B) In `lib/plant-labels.ts`, reconcile the DATA HAZARD (RESEARCH Pitfall 1): replace the line-9 `PERM_USE_OPTIONS` array contents in place with the exact D-02 16-tag vocabulary: `'nitrogen fixer','dynamic accumulator','insectary plant','chop-and-drop','wildlife benefit','medicinal','fiber','groundcover','windbreak','pollinator nectary','bee forage','living mulch','biomass producer','erosion control','hedgerow','edible'`. Add `export const FUNCTIONAL_ROLE_OPTIONS: string[]` as the canonical name with the same 16-tag array, and keep `PERM_USE_OPTIONS` as `export const PERM_USE_OPTIONS = FUNCTIONAL_ROLE_OPTIONS` (alias — preserves the LIVE v1 filter sidebar importer `FilterControls.tsx:7,40` and `permUses` query path; do NOT delete or rename the export, RESEARCH wrongly claimed no importers; D-04: this is import-preservation only, not a new filter control). Legacy `pioneer` is intentionally dropped from roles (it is a `succession_role` value under D-09); `wildlife habitat`→`wildlife benefit`, `pollinator`→`pollinator nectary`/`bee forage`, `biomass`→`biomass producer`, `insectary`→`insectary plant` per D-02.

  (C) Also add the remaining vocab/label constants (used by Plan 02-02 script and Plan 02-03 display): `SUCCESSION_OPTIONS = ['pioneer','early successional','mid successional','climax']` (D-09), `ESTABLISHMENT_OPTIONS = ['easy','moderate','challenging']` (D-10), `MAINTENANCE_OPTIONS = ['low','moderate','high']` (D-11), `PROPAGATION_OPTIONS = ['seed','cutting','division','layering','grafting','root cutting','tuber','sucker']` (D-13), `EDIBLE_PART_OPTIONS = ['leaf','fruit','nut','seed','root','flower','bark','sap','shoot','pod']` (D-14). MONTH_OPTIONS (line 4) is reused for harvest (D-15) — do NOT duplicate. Add the 4 InfoCell display-label constants per UI-SPEC lines 107-114 (e.g. a `FUNCTIONAL_INFO_LABELS` map or individual consts): `forest_garden_layer`→`Forest Garden Layer` (D-08), `establishment_difficulty`→`Establishment`, `maintenance_level`→`Maintenance`, `years_to_bearing`→`Years to Bearing`. Match the existing `export const NAME: string[] = [...]` shape; no inlining in the page (Phase 1 D-09, UI-SPEC line 114).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "succession_role: string\[\] | null" lib/types.ts && grep -q "FUNCTIONAL_ROLE_OPTIONS" lib/plant-labels.ts && grep -q "'pollinator nectary'" lib/plant-labels.ts && grep -q "PERM_USE_OPTIONS = FUNCTIONAL_ROLE_OPTIONS" lib/plant-labels.ts && ! grep -E "PERM_USE_OPTIONS: string\[\] = \['nitrogen fixer', 'dynamic accumulator', 'edible', 'medicinal', 'pollinator'," lib/plant-labels.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (strict mode; Plant interface compiles; FilterControls.tsx still resolves PERM_USE_OPTIONS)
    - `npm run lint` exits 0
    - `lib/types.ts` contains all 7 new fields with exact names matching the migration; `forest_garden_layer`/`permaculture_uses` not duplicated
    - `lib/plant-labels.ts` contains `FUNCTIONAL_ROLE_OPTIONS` with exactly the 16 D-02 tags; `'pollinator nectary'`, `'bee forage'`, `'biomass producer'`, `'wildlife benefit'`, `'insectary plant'`, `'hedgerow'` present; legacy `'wildlife habitat'`, `'pollinator'` (bare), `'biomass'` (bare), `'pioneer'` NOT present in the roles array
    - `PERM_USE_OPTIONS` still exported (as alias of FUNCTIONAL_ROLE_OPTIONS) — grep `PERM_USE_OPTIONS = FUNCTIONAL_ROLE_OPTIONS` succeeds; FilterControls.tsx import unbroken (D-04 — no new filter control introduced)
    - `SUCCESSION_OPTIONS`, `ESTABLISHMENT_OPTIONS`, `MAINTENANCE_OPTIONS`, `PROPAGATION_OPTIONS`, `EDIBLE_PART_OPTIONS` exported with exact D-09/D-10/D-11/D-13/D-14 values
    - 4 InfoCell label constants present (`Forest Garden Layer`, `Establishment`, `Maintenance`, `Years to Bearing`)
  </acceptance_criteria>
  <done>D-08: ForestGardenLayer type unchanged. Plant interface + all vocab/label constants compile under strict TS; lint clean; PERM_USE_OPTIONS reconciled to D-02 vocab with v1 filter importer preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live Postgres | DDL applied to production DB via `supabase db push` |
| (no user-input boundary in this plan) | Schema/type/constants only — no runtime input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | migration DDL | accept | Single ADD COLUMN migration mirroring the reviewed `...612` precedent; CHECK constraints enforce single-value enum integrity at the DB layer; no data mutation |
| T-02-02 | Denial of Service | `supabase db push` on prod | accept | `ADD COLUMN` with no default and no backfill is a metadata-only operation on Postgres (no table rewrite/lock escalation); ~250-row table |
| T-02-04 | Access Control | new columns RLS | accept | New columns inherit existing `plants` RLS SELECT `USING (true)` (public catalog, intended per RESEARCH Security Domain V4); no RLS change needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict; Plant + plant-labels + all live importers compile)
- `npm run lint` exits 0
- Migration file present with exact column set; no duplicate `forest_garden_layer`/`permaculture_uses`
- [BLOCKING] migration applied to live Supabase DB (human-confirmed)
- `PERM_USE_OPTIONS` still exported and resolvable by `FilterControls.tsx`
</verification>

<success_criteria>
- 7 new columns exist in the live `plants` table
- `Plant` interface has the 7 new fields, compiles strict
- `PERM_USE_OPTIONS` reconciled to the D-02 16-tag vocab without breaking the v1 filter sidebar
- All Plan 02-02/02-03 vocab + label constants exported from `lib/plant-labels.ts`
</success_criteria>

<output>
Create `.planning/phases/02-functional-data-enrichment/02-01-SUMMARY.md` when done
</output>
</output>
