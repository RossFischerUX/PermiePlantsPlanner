---
phase: 03-companion-planting-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - scripts/verify-relationships.ts
  - package.json
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "lib/types.ts exports PlantRelationship, RelationshipType, RelationshipConfidence"
    - "D-08: PlantRelationship type declares mechanism: string | null (nullable no-documented-mechanism); relationship_type/confidence non-optional"
    - "npm run build compiles with the new types under TS strict, zero errors, no any"
    - "scripts/verify-relationships.ts exists and fails loudly (exit 1) until the seeded table exists"
    - "D-04: The exact curated seed pair list is ~10–20 hand-curated pairs (not the v2 backfill dataset), drawn only from plant names confirmed present in the live plants catalog"
  artifacts:
    - path: "lib/types.ts"
      provides: "PlantRelationship interface + RelationshipType/RelationshipConfidence string unions"
      contains: "export interface PlantRelationship"
    - path: "scripts/verify-relationships.ts"
      provides: "Service-role honest-gate verification of success criterion #3 + D-06 coverage"
      contains: "process.exit(1)"
    - path: ".planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md"
      provides: "Confirmed-present curated pair list (resolved plant ids + exact name strings) feeding Plan 02's seed migration"
      contains: "subject"
  key_links:
    - from: "scripts/verify-relationships.ts"
      to: "lib/types.ts"
      via: "import type { PlantRelationship }"
      pattern: "import type .* PlantRelationship"
    - from: "03-SEED-MANIFEST.md"
      to: "plants table (live)"
      via: "name-resolved plant ids confirmed by live query"
      pattern: "Solanum|Juglans|Ocimum"
---

<objective>
Establish the type contract, the failing verification harness, and the live-catalog ground truth that the end-to-end slice depends on.

This is the contract + failing-test stage of the MVP slice. After this plan: `PlantRelationship` is exported and compiling, `scripts/verify-relationships.ts` exists and correctly FAILS LOUD (the table does not exist yet — RED state), and a SEED-MANIFEST records the exact curated pairs proven to resolve against the live `plants` catalog. This de-risks the single biggest execution hazard (RESEARCH.md A1: classic companion species may be absent from the iNaturalist-imported production catalog) BEFORE any migration is written.

Purpose: COMP-02 type integration; resolve the A1 live-catalog risk before the seed is authored so Plan 02's fail-loud migration cannot hard-fail on a missing plant.
Output: `lib/types.ts` additions, `scripts/verify-relationships.ts`, `package.json` script entry, `03-SEED-MANIFEST.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-companion-planting-schema/03-CONTEXT.md
@.planning/phases/03-companion-planting-schema/03-RESEARCH.md
@.planning/phases/03-companion-planting-schema/03-PATTERNS.md

<interfaces>
<!-- Existing lib/types.ts conventions the executor MUST mirror. Extracted from codebase. -->

From lib/types.ts (string-union scalar type aliases — no semicolons, single-line):
```typescript
export type Sun = 'full sun' | 'part shade' | 'full shade'
export type Water = 'low' | 'moderate' | 'high'
export type ForestGardenLayer = 'canopy' | 'sub-canopy' | 'shrub' | 'herb' | 'ground cover' | 'rhizosphere' | 'climber'
```

From lib/types.ts (interface conventions — 2-space indent, no semicolons, `string | null` for nullable, optional joined row mirrors PlantListItem.plant?):
```typescript
export interface PlantListItem {
  id: string
  list_id: string
  plant_id: string
  sort_order: number
  notes: string | null
  created_at: string
  plant?: Plant
}
```

verify-relationships.ts scaffold analog: scripts/backfill-usda-zones.ts (shebang, JSDoc header with Usage + .env.local requirements, `SUPABASE_SECRET_KEY ?? SUPABASE_SERVICE_ROLE_KEY!` fallback ordering, `if (!SERVICE_ROLE_KEY) throw`, `if (error) throw new Error(...)`, `main().catch(err => { console.error('\nFatal:', err); process.exit(1) })`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PlantRelationship type + write the failing verification harness</name>
  <files>lib/types.ts, scripts/verify-relationships.ts, package.json</files>
  <read_first>
    - lib/types.ts (the file being modified — existing Sun/ForestGardenLayer unions + Plant/PlantListItem interface style)
    - scripts/backfill-usda-zones.ts (verbatim scaffold analog: shebang, JSDoc header, dotenv, service-role key fallback, typed result, top-level catch + exit code)
    - .planning/phases/03-companion-planting-schema/03-RESEARCH.md (Pattern 3 type shape; Code Examples — verification script; Pitfall 4 TS-strict union narrowing)
    - .planning/phases/03-companion-planting-schema/03-PATTERNS.md (lib/types.ts append rules; verify-relationships scaffold reuse)
    - .planning/phases/03-companion-planting-schema/03-CONTEXT.md (D-06 coverage requirements; D-08 mechanism nullable)
  </read_first>
  <behavior>
    - npm run build exits 0 with PlantRelationship/RelationshipType/RelationshipConfidence in scope (TS strict, no `any`)
    - `npx tsx scripts/verify-relationships.ts` run NOW (before any migration) exits 1 and prints a clear failure (plant_relationships table does not exist) — RED state, proves the harness actually gates
    - The verify script's honest-gate assertions encode D-06: ≥1 HELPS row for the probe plant, ≥1 AVOIDS row, all three confidence levels present somewhere in the seed, every row has non-empty mechanism unless explicitly null, both plant ids populated
  </behavior>
  <action>
    Append to `lib/types.ts` after `PlantListItem`, mirroring the existing single-line union + 2-space interface style (no semicolons): `export type RelationshipType = 'HELPS' | 'AVOIDS'`; `export type RelationshipConfidence = 'verified' | 'traditional' | 'anecdotal'`; and `export interface PlantRelationship` with fields `id: string`, `subject_plant_id: string`, `object_plant_id: string`, `relationship_type: RelationshipType`, `confidence: RelationshipConfidence`, `mechanism: string | null` (D-08 — `relationship_type`/`confidence` are required non-optional fields with no default semantics in the type; `mechanism: string | null` is the nullable honest "no documented mechanism" mechanism, NOT optional `?`), `created_at: string` (string not Date, matches every existing interface). Do NOT add optional joined `subject_plant?`/`object_plant?` fields — the verification query does not embed a PostgREST join (Phase 3 has no UI consumer; keep the type minimal per 03-PATTERNS.md).

    Create `scripts/verify-relationships.ts` reusing the `scripts/backfill-usda-zones.ts` scaffold verbatim: shebang `#!/usr/bin/env tsx`, JSDoc header (one-line purpose + `Usage: npx tsx scripts/verify-relationships.ts` + `.env.local` requirement note), `import { createClient } from '@supabase/supabase-js'`, `import * as dotenv from 'dotenv'`, `import type { PlantRelationship } from '../lib/types'`, `dotenv.config({ path: '.env.local' })`, `const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL!`, `const SERVICE_ROLE_KEY = process.env.SUPABASE_SECRET_KEY ?? process.env.SUPABASE_SERVICE_ROLE_KEY!` (this exact fallback ordering), `if (!SERVICE_ROLE_KEY) throw new Error(...)`, `const supabase = createClient(SUPABASE_URL, SERVICE_ROLE_KEY)`. Drop any Anthropic import. Implement `main()` per 03-RESEARCH.md Code Examples: resolve probe plant tomato by `.ilike('common_name','tomato').single()` (throw if not found); fetch all relationships where tomato is subject OR object via `.or(\`subject_plant_id.eq.${id},object_plant_id.eq.${id}\`).returns<PlantRelationship[]>()` (typed, no `any`, no `as any`); accumulate `const fails: string[] = []` with these honest-gate assertions — `rows.length >= 2`, at least one `relationship_type === 'HELPS'`, at least one `=== 'AVOIDS'`, every row has truthy `subject_plant_id`, `object_plant_id`, `relationship_type`, `confidence`, and `mechanism === null || mechanism.trim() !== ''`; then a global query `.from('plant_relationships').select('confidence').returns<{confidence:string}[]>()` asserting the set of confidence values contains all of `verified`, `traditional`, `anecdotal` (D-06). If `fails.length` print each with `✗ ` prefix and `process.exit(1)`; else print `✓ N tomato relationships, all fields populated, D-06 coverage met.` and exit 0. Tail with `main().catch(err => { console.error('\nFatal:', err); process.exit(1) })`. Note in a comment that the `.or()`/global selects are well under the PostgREST 1000-row cap at ~15-pair seed scale (no `.range()` pagination needed; CLAUDE.md cap awareness documented). Add to `package.json` `scripts`: `"verify-relationships": "tsx scripts/verify-relationships.ts"`, placed after `"dedupe-plants"`, mirroring existing entry style. The probe plant common-name string ('tomato') MUST match the SEED-MANIFEST string produced in Task 2 — if Task 2 confirms a different probe plant, update this script's `.ilike(...)` accordingly before finishing the plan.
  </action>
  <verify>
    <automated>cd /Users/rossfischer/Desktop/Development/plantmaster-clone && npm run build 2>&1 | tail -5 && grep -q "export interface PlantRelationship" lib/types.ts && grep -q "import type { PlantRelationship }" scripts/verify-relationships.ts && grep -q '"verify-relationships"' package.json && (npx tsx scripts/verify-relationships.ts; test $? -eq 1 && echo "HARNESS-RED-OK")</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (TS strict compile passes — success criterion #2)
    - `grep -q "export interface PlantRelationship" lib/types.ts` succeeds; `lib/types.ts` contains `mechanism: string | null` and no `subject_plant?`/`object_plant?` fields
    - D-08: `lib/types.ts` declares `mechanism: string | null` (nullable, NOT optional `mechanism?`); `relationship_type` and `confidence` are required non-optional fields
    - `lib/types.ts` contains `export type RelationshipType = 'HELPS' | 'AVOIDS'` and `export type RelationshipConfidence = 'verified' | 'traditional' | 'anecdotal'`
    - `scripts/verify-relationships.ts` contains `import type { PlantRelationship }`, `.returns<PlantRelationship[]>()`, and `process.exit(1)`; contains no `as any` and no untyped `: any`
    - `package.json` scripts block contains `"verify-relationships": "tsx scripts/verify-relationships.ts"`
    - Running `npx tsx scripts/verify-relationships.ts` NOW exits with code 1 (table absent — harness is RED and actually gates)
  </acceptance_criteria>
  <done>Types exported and compiling; verification harness exists and fails loud against the not-yet-created table (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Resolve A1 — confirm classic companion species exist in the live plants catalog and write the SEED-MANIFEST</name>
  <files>.planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md</files>
  <read_first>
    - .planning/phases/03-companion-planting-schema/03-RESEARCH.md (Assumptions Log A1; Pitfall 1 silent empty seed; Pitfall 2 TOO_MANY_ROWS; Open Question 1)
    - .planning/phases/03-companion-planting-schema/03-CONTEXT.md (D-04 ~10–20 pairs; D-06 coverage proof; specific Three Sisters / black walnut / tomato↔basil examples)
    - scripts/backfill-usda-zones.ts (service-role client + dotenv pattern for an ad-hoc resolve query)
    - CLAUDE.md (PostgREST 1000-row cap; service-role key is .env.local only, scripts-only, never client-exposed)
  </read_first>
  <action>
    D-04: produce a small (~10–20) HAND-CURATED directed pair list — this is NOT the v2 AI-backfill dataset, it is the deliberately limited Phase-3 hand-seed. Query the LIVE production `plants` catalog (service-role, via a throwaway `npx tsx` one-liner or an inline script using the same `.env.local` + `createClient` pattern as `scripts/backfill-usda-zones.ts` — do NOT hard-code or echo the service-role key) to resolve each candidate companion species by exact `common_name ILIKE '<name>'` OR `latin_name ILIKE '<latin>'`. Candidate species to probe (RESEARCH.md / D-04 / specifics): tomato (Solanum lycopersicum), basil (Ocimum basilicum), black walnut (Juglans nigra), corn / maize (Zea mays), bean / common bean (Phaseolus vulgaris), squash / pumpkin (Cucurbita pepo), and any additional well-documented classic companions you need to satisfy D-06 (≥1 HELPS, ≥1 AVOIDS, all three confidence levels, every row non-empty mechanism). For each name record: present? (1 row / 0 rows / >1 rows ambiguous), the resolved `id` and exact `common_name`/`latin_name`, and the disambiguating exact string to use in the seed (prefer the precise latin name to avoid TOO_MANY_ROWS against the ~1455-row catalog — Pitfall 2).

    Build the final curated D-04 pair list (~10–20 directed `subject → object : type (confidence)` rows with mechanism prose — bounded hand-seed, not the v2 dataset) using ONLY confirmed-present, unambiguously-resolvable plants. The list MUST satisfy D-06: ≥1 HELPS, ≥1 AVOIDS, each of `verified`/`traditional`/`anecdotal` used ≥1, every row non-empty mechanism text, and the probe plant (tomato, or a substitute if tomato is absent) appears in ≥2 rows spanning both HELPS and AVOIDS. Write all of this to `.planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md` as a table: subject (exact resolve string + latin), object (exact resolve string + latin), relationship_type, confidence, mechanism prose. Also record the chosen probe plant common-name string for the verify script.

    If a key species is ABSENT or irreducibly AMBIGUOUS such that D-06 cannot be met from confirmed-present plants: do NOT weaken D-05's fail-loud guard and do NOT silently drop coverage. Substitute with a different classic companion pair whose species ARE present and still satisfies D-06. Record substitutions and the reason in the SEED-MANIFEST under a `## Substitutions` heading. If NO viable D-06-satisfying set can be built from present plants at all, STOP and surface to the user as a decision (substitute-pairs-vs-add-companion-plants-to-the-catalog) — write the blocker into the SEED-MANIFEST `## BLOCKER` section and do not proceed. After finalizing, if the probe plant differs from 'tomato', update `scripts/verify-relationships.ts` `.ilike('common_name', ...)` to match (consistency with Task 1).
  </action>
  <verify>
    <automated>cd /Users/rossfischer/Desktop/Development/plantmaster-clone && test -f .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "subject" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "HELPS" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "AVOIDS" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "verified" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "traditional" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && grep -Eiq "anecdotal" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md && ! grep -q "## BLOCKER" .planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - D-04: `.planning/phases/03-companion-planting-schema/03-SEED-MANIFEST.md` exists with a ~10–20 row hand-curated pair table (deliberately bounded Phase-3 seed, not the v2 backfill dataset); every subject and object names a confirmed-present plant with an exact resolve string (common or latin) recorded
    - The manifest pair set contains ≥1 row with `HELPS` and ≥1 with `AVOIDS`; the strings `verified`, `traditional`, `anecdotal` each appear ≥1 (D-06 coverage)
    - The chosen probe plant appears in ≥2 manifest rows spanning both relationship types; the probe common-name string is recorded and matches `scripts/verify-relationships.ts` `.ilike(...)`
    - No `## BLOCKER` section present (a present BLOCKER means the plan must stop and escalate to the user — not pass)
    - Service-role key was read from `.env.local` only and never written into any committed file (grep the repo diff: no `SUPABASE_SERVICE_ROLE_KEY=` literal value, no `SUPABASE_SECRET_KEY=` literal value)
  </acceptance_criteria>
  <done>SEED-MANIFEST records a D-06-satisfying curated pair list drawn exclusively from species confirmed present in the live catalog; A1 risk resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → live Supabase (service-role) | Task 2 ad-hoc resolve query + Task 1 verify harness read the production DB with the service-role key |
| `.env.local` → committed repo | Service-role key must never leak into committed files (verify script, manifest, package.json) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | service-role key used by Task 2 resolve query / `scripts/verify-relationships.ts` | mitigate | Key read from `.env.local` via `dotenv` only (mirrors `scripts/backfill-usda-zones.ts`); never hard-coded, never echoed, never committed; `scripts/` excluded from Next build (CLAUDE.md). Acceptance criterion greps the diff for literal key values. |
| T-03-02 | Tampering | `lib/types.ts` introducing loose `any` that weakens the v2 backfill type contract | mitigate | `PlantRelationship` uses the `RelationshipType`/`RelationshipConfidence` string unions; `npm run build` (TS strict) gates; acceptance criterion greps for `as any`/`: any`. |
| T-03-03 | Repudiation / false-pass | Verify harness silently passing over an empty table | mitigate | Harness is proven RED in Task 1 (`exit 1` while table absent) before it is ever trusted GREEN; honest-gate `fails[]` + `process.exit(1)` (D-05 / honest-gates principle). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — RESEARCH.md Package Legitimacy Audit is N/A (zero new dependencies; all tooling already in package.json). No slopcheck required. |
</threat_model>

<verification>
- `npm run build` exits 0 (TS strict, success criterion #2 type compile)
- `scripts/verify-relationships.ts` exits 1 when run before the migration (harness proven to gate — RED)
- `03-SEED-MANIFEST.md` exists, satisfies D-06 coverage, has no `## BLOCKER`
- No service-role key literal in any committed file
</verification>

<success_criteria>
- COMP-02 type contract present and compiling under TS strict (success criterion #2 partially — full proof in Plan 02 after the query runs against real rows)
- The A1 live-catalog risk is resolved: the seed pair list is locked to confirmed-present plants before any migration is authored
- The verification harness exists and is proven to fail loud (RED), ready to turn GREEN in Plan 02
</success_criteria>

<output>
Create `.planning/phases/03-companion-planting-schema/03-01-SUMMARY.md` when done. Record the final SEED-MANIFEST pair list and chosen probe plant so Plan 02 can author the seed migration verbatim from it.
</output>
</output>
